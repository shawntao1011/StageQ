[project]
name = "stageq"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pyyaml>=6.0",
]

[project.scripts]
stageqsvc = "stageq.cli.svc_main:main"
stageqjob = "stageq.cli.job_main:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"